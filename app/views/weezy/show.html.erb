<%= link_to "Next Song", weezy_path, class: "btn btn-primary col-xs-4 col-xs-offset-4" %>

<div class="content text-center">
  <%= raw @song.lyrics.first %>
  <%= raw @song.lyrics[1] %>
  <%= raw @song.lyrics.last %>
</div>
